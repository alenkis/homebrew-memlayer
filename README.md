# homebrew-tap

Homebrew tap for [memlayer](https://github.com/alenkis/memlayer).

## Install

```sh
brew install alenkis/tap/memlayer
```

## Requirements

- Java 22+ (installed automatically via `openjdk` dependency)
